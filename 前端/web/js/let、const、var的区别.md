# let、const、var的区别

### var

1. 会将变量存在window
2. 没有块级作用域的概念
3. 有全局作用域的概念
4. 不初始化值，默认为undefined
5. 存在变量提升（将变量的声明部分提升到当前作用域的最顶端）

### let

1. 有块级作用域
2. 不存在变量提升（x is not defined）
3. 存在暂时性死区-TDZ（未赋值时使用会报错）
4. 同一作用域中不存在重复声明

### const

1. 必须立即初始化，不能后面再赋值
2. 常量的值不能改变（内存地址不能改变）
