# 原型和原型链

![[微信截图_20240312020038.png]]

### 构造函数

```js
function Person(name) {
  //大写驼峰命名
  // var this = {};//省略
  this.name = name;
  // return this;//省略
}
const person = new Person("John");
```

### 原型

- 每个函数都会自动附带一个属性 **prototype** ,这个属性的值是一个普通对象，称之为原型对象

### 实例

- **instance**,通过**new**产生的对象统称为实例。严格来说js中所有的对象都称为实例。
- 每个实例都有一个特殊的属性 ****proto****，称之为隐式原型，指向构造函数的原型。
- 当访问实例成员时，先找自身，如果不存在，会自动从隐式原型中寻找

### 原型链

- 隐式原型往上查找形成一条链条，原型链
- **Object.prototype.**​****proto**** 比较特殊，固定指向null
- **Object.**​****proto**** === **Function.**​****proto****
  ![[Pasted image 20240313020245.png]]

### 面试题

###### 创建一个没有隐式原型的对象

- **Object.create(null)**
- **Object.setPrototype(对象,null--原型)**  === **obj.**​****proto****​ ** = null**
- **obj.**​****proto****​ ** = null** ,最好不用

##### 说一说你对js中原型和原型链的理解？

- 每一个对象都有一个__proto__属性，该属性指向自己的原型对象。原型对象也有自己的原型对象，直到指向null。这个过程就是原型链
- 每一个构造函数都有一个prototype属性，该属性指向实例对象的原型对象
- 原型对象里的constructor指向构造函数本身

##### 对一个构造函数实例化后，它的原型链指向什么？

- 指向该构造函数实例化出来对象的原型对象
- 对于构造函数来说，可以通过prototype访问到该对象
- 对于实例对象来说，可以通过隐式属性__proto__来访问
